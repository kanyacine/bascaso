services:
  bascaso:
    image: ghcr.io/kanyacine/bascaso:latest
    # build: .  # uncomment to build locally
    ports:
      - "3000:3000"
      - "3100:3100" # MCP server
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
